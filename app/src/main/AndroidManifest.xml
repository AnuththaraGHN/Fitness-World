<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fitnessworld"
    tools:ignore="ExtraText">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessWorld"
        tools:ignore="AllowBackup,WrongManifestParent">
        <activity android:name=".MainActivity3"
            android:exported="true"
            tools:ignore="IntentFilterExportedReceiver">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".Home" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".Search" />
        <activity android:name=".MealPlanActivity" />
        <activity android:name=".MealPlan" />
        <activity android:name=".GenatagebmiActivity" />
        <activity android:name=".Genatagebmi" />
        <activity android:name=".BMICalculatorActivity"/>
        <activity android:name=".BMICalculator"/>
        <activity android:name=".MainActivity"/>



    </application>

</manifest>